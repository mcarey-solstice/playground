###
#
##

language: bash

install: true

env:
  global:
    - 'MESSAGE=$(if [ $TRAVIS_PULL_REQUEST != "false" ]; then echo "hello world"; else echo "goodbye world")'

deploy:
  provider: script
  script:
    - echo $MESSAGE
  on:
    repo: mcarey-solstice/playground
    all_branches: true
# deploy

after_deploy:
  - echo "After the deployment"
# after_deploy

# .travis.yml
